qr_corrections:
  # default 表示“只要扫到二维码，就先按这个已知定位点把地图坐标矫正一次”。
  # 这里先对齐到 C 区固定路线入口，避免把车体贴到地图原点导致 costmap 越界。
  # 如果后续不同二维码对应不同扫码点，可以继续往下追加同名键，例如 "171838"、"739269"。
  default:
    x: 2.50
    y: 2.03
    yaw: 1.57
